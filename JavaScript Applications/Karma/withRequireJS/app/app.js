(function() {
	require.config({
		baseUrl: 'app/libs',
    	paths: {
        	jquery: 'jquery',
        	mustache: 'mustache',
        	q: 'q',
        	underscore: 'underscore'
    	}
	});

	require([], function() {
		
	})
}())