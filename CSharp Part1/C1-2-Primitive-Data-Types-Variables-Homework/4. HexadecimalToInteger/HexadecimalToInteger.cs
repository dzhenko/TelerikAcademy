using System;

class HexadecimalToInteger
{
    static void Main()
    {
        int decimalnumber = (int)0xFE;            //FE is 254 from Hex to dec and 0x is infront of every hex value
        Console.WriteLine(decimalnumber);           
    }
}

